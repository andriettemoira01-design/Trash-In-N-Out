{
  "name": "functions",
  "description": "Cloud Functions for Trash-In-N-Out push notifications",
  "scripts": {
    "build": "tsc",
    "serve": "npm run build && firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions"
  },
  "main": "lib/index.js",
  "dependencies": {
    "firebase-admin": "^11.11.0",
    "firebase-functions": "^4.5.0"
  },
  "devDependencies": {
    "typescript": "^5.1.6"
  },
  "engines": {
    "node": "18"
  },
  "private": true
}
